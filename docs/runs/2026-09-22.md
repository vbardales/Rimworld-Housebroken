# Runs of 2026-09-22

Headless Linux game through `scripts/Run-PickleWsl.ps1` and the machine queue. Evidence under
`Tests/Pickle/evidence/2026-09-22/` (ignored by git), **minified on 2026-09-24**: `summary.md`, `junit.xml`,
`log-check.txt` and the captures named below. Times are the machine log's `LOCK` to `UNLOCK`.

| Time | Run | Result | Evidence folder | What it showed |
| --- | --- | --- | --- | --- |
| 17:44-17:46 | `runtime-evidence`, English, PID 33800 | `passed`: 3 passed, 0 failed, 1 skipped (RIMMSQOL not staged) | `runtime-evidence-English/`: summary, JUnit, log check, `language-page.jpg`, `opened-by-mainbuttons.jpg` | The native options page draws in English, unclipped. The hidden shortcut is absent on a clean configuration, is drawn once revealed, and opens the same page. Log check: 0 and 0 |
| before 23:19 | French and RIMMSQOL tickets, PIDs 38980 and 13640 | **no scenario played** | none | Both processes disappeared before taking the `LOCK`. Not a result |
| 23:19-23:24 | `runtime-evidence`, French, PID 28556 | `passed`: 3 passed, 0 failed, 1 skipped (RIMMSQOL not staged) | `runtime-evidence-French/`: same files | The French page is legible: accents and slider labels correct, no raw key, nothing clipped. The shortcut opens the same French page. Log check: 0 and 0 |
| 23:27-23:29 | `avec-rimmsqol`, English, PID 13800 | `passed`: 1 passed, 0 failed | `avec-rimmsqol-English/`: summary, JUnit, log check, `opened-after-rimmsqol-reveal.jpg` | RIMMSQOL lists `Housebroken_Settings` hidden, reveals it, the main bar draws it, activating it opens the Housebroken page, then hide and forget both hold. Log check: 0 and 0 |

## What stands, and what does not

- **Shown:** the native settings page opens and draws in English and in French, the MainButtons shortcut is hidden by
  default and opens the same page, and RIMMSQOL can reveal and hide it. Every capture of the three runs was opened
  and reviewed before it was minified.
- **Not shown:**
  - that a written setting survives on disk: `01-settings.feature` sets `outdoorFactor`, writes, reopens and re-reads
    the in-memory value, so a broken write would still pass;
  - any animal behavior, a new colony, an existing save, or persistence across a game restart (TF-01 to TF-22);
  - that RIMMSQOL keeps a visibility choice across a restart, which is RIMMSQOL's own behavior;
  - the French label and tooltip of the shortcut once revealed: the RIMMSQOL pass ran in English only.
- **Dropped on 2026-09-24, as duplicates:** the capture of scenario 01 in the English and in the French run, each
  showing the dialog the language capture of the same run already shows.
