# Runs of 2026-09-24

The first runs of the functional scenarios (TF-01 to TF-22), played on a proving ground the suite builds on the fixture
map. Headless Linux game through `scripts/Run-PickleWsl.ps1` and the machine queue. Evidence under
`Tests/Pickle/evidence/2026-09-24/` (ignored by git), minified to `summary.md` and `junit.xml`.

| Time | Run | Result | Evidence folder | What it showed |
| --- | --- | --- | --- | --- |
| 13:07-13:12 | `10` to `14`, English, no tools staged, PID 49660 | `failed`: **0 passed, 15 failed** | `tf-01-14-English/` (summary and JUnit) | **A fault of the test, not of the mod.** All fifteen scenarios stopped in their Background on the same line: the roofed room meant to stay outside the home area was inside it. The game marks home area around every building of the player each time one is spawned, so the walls of the second room pulled it in. Nothing about the mod was exercised. The yard now applies its home areas once every wall stands, and its walls belong to nobody. The launcher also failed to copy one failure screenshot, whose name went past the Windows path limit: scenario names are shorter |

## What stands, and what does not

- **Nothing about the mod is shown by this run.** The first run that reaches a `Then` step is the next one.
- The next attempt plays features 10 to 14, 20, 28, 29 and 32 in one launch, with the tools of `wsl-deps.settings.map`
  staged and the hover tool left out on purpose: its hook runs before every scenario of a run that stages it, so it
  has a pass of its own.
