# Runs of 2026-09-25

The functional scenarios TF-01 to TF-14, played by Pickle in a headless Linux game through the ticket dispatcher, one small
ticket per group. Evidence under `Tests/Pickle/evidence/2026-09-25/` (ignored by git), minified to `summary.md`, `summary.json`,
`junit.xml` and a `log-check.txt`. Tree of these runs: repository `f441a99`; `Housebroken.dll` sha256 `8DF40BAE...`, steps assembly
`F8F65C68...`. The game build of these three runs was not read from the log before it was dropped. The runs of the second batch below ran on **RimWorld 1.6.4871 rev600** (the fixture was saved by 1.6.4633, hence the version warning).

| Ticket | Run | Result | Evidence folder | What it showed |
| --- | --- | --- | --- | --- |
| `20260925-000813-690-ea4f` | features 10 and 11: TF-01 to TF-06, English | `passed`: **6 of 6**, 0 skipped | `run-tf01-06/` | The defaults, the seven-row factor table, the six steps of learning and losing training, the sliders and the no-reduction case, the eligible animals and the catalyst. Log check: 0 and 0 |
| `20260925-000814-160-8b16` | features 12 and 13: TF-07 to TF-12, English | `passed`: **7 of 7**, 0 skipped | `run-tf07-12/` | The six places of the base, whole-home mode, the indoor and outdoor factors, manure while moving, carried mud and blood, the independence of the two options. Log check: 0 and 0 |
| `20260925-000814-700-c865` | feature 14: TF-13 and TF-14, English | `passed`: **2 of 2**, 0 skipped | `run-tf13-14/` | The alert exemption and the mixed list. Log check: 0 and 0 |
| `20260925-133613-530-a77c` | `::TF-15 change, cancel reset, confirm reset`, English, settings pass | `passed`: 1 of 1 | `x-tf15-reset/` (and `reset-confirmation.jpg`) | The reset button opens the confirmation; going back changes nothing; confirming restores every default; closing applies them. The capture was opened: the question, Go back and Confirm are legible, and the page behind still shows the changed values. Log check: 0 and 0 |
| `20260925-133618-882-d65e` | `::TF-17 carried mud survives save and reload`, English | `passed`: 1 of 1 | `x-tf17-reload/` | The carried mud is still carried after the game saves and reloads; held indoors, released outdoors. Log check: 0 and 0 |
| `20260925-133621-496-8f31` | `::TF-18 a game saved with the mod holds no data of it`, English | `passed`: 1 of 1 | `x-tf18-save/` | The save holds no mention of Housebroken outside its header, which lists the active mods. This is the first half of TF-18; loading it without the mod is a later pass. Log check: 0 and 0 |
| `20260925-133624-356-d929` | `::TF-19 a reused ID gets its own factor`, English | `passed`: 1 of 1 | `x-tf19-games/` | An animal of a second game, given the ID of a reduced animal of the first, has its own factor at once and after 251 ticks. The first game had advanced the clock, so the reload moved it back: the case the mod guards against. Log check: 0 and 0 |
| `20260925-133627-045-30cf` | `::TF-19 the rate follows the map` (caravan), English | `failed`: 0 of 1 | (deleted, superseded) | **A fault of the test, not of the mod.** The step that founds a second colony found no valid tile within 40 tiles of the first map. Nothing about the caravan was reached. The step now walks every tile of the world by distance and, when the game refuses them all, reports the reasons it gives |
| `20260925-170650-037-4b7b` | `::TF-19 the rate follows the map` (caravan), English | `failed`: 0 of 1 | (deleted, superseded) | **A fault of the test, not of the mod.** The second map was founded and the caravan rate read (7.2 on the caravan), then the yard step found no clear 4x5 rectangle on the west edge of the map the game had drawn. The search now takes the rectangle that costs the fewest cells to clear (`c75b890`) |
| `20260925-191926-513-6029` | `::TF-19 the rate follows the map` (caravan), English | `passed`: 1 of 1 | `y3-tf19-caravan/` | Replay after the fix, on `c75b890`, 15 s, no attachment beyond the report (the scenario takes no capture). The elephant is 0 times its base rate in the closed room, its rate is readable on the caravan, 0.3 times on the second map after arrival, 0 in that map's closed room and 0.3 outdoors, and the first map is back after the trip. Log check: no error after the scenario. The game load logs one error, `Pickle tests did not load any content` (the companion mod holds only steps), as in every earlier run |

## What stands, and what does not

- **Shown:** every scenario of TF-01 to TF-14 passed on its first attempt, on the game own stat, the game own step-by-cell entry
  point and the game own alert, with the two DLC that the scenarios need (Biotech for the mechanoid, Odyssey for the catalyst) active.
- **Not shown, and recorded as such in `TESTS_FONCTIONNELS.md`:**
  - the slider bounds in the interface (TF-04): only the offline normalization tests cover them;
  - the pathfinder: the animals are teleported from cell to cell, and every roll after that is the game code (TF-10 to TF-12);
  - a click on an alert entry (TF-14): names and targets are compared in code;
  - a colony started from scratch (TF-01): the colony is the fixture.
- **Not run yet:** TF-15 to TF-22, and the French run of the explanation lines.